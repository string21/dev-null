// for types we define, we capitalized the first letter to distinguish them from types defined in the standard library

// examples are string, int, double
// user defines type examples: Car, Person, Something