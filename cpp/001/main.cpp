// most compliers can warn about narrowing --- perharps i just need to adhere to those to avoid issues

// regardin initialization of variables, use {} or ={} if you want compile-time protections against narrowing